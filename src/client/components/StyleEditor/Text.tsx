import * as React from "react"

import { Icons } from "client/components"
import { RawStyle } from "models"

interface IProps {
    style: RawStyle,
}

export function TextEditor(props: IProps) {
    return (
        <div className="text-editor">
            
        </div>
    )
}
